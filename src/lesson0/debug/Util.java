package lesson0.debug;

public class Util {
    public static double sum(double y1, double x1, double y2, double x2) {
        boolean b = (y1 + x1) != 0;
        return y1 + x1 + y2 + x2;
    }

    public static double multi(double a, double b) {
        return a * b;
    }
}
